"""Domain models shared across provider adapters.

Models carry provider *external* ids; resolution to canonical internal ids
happens in the repository layer via the provider_entity_map crosswalk.
"""
